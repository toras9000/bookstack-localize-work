#!/usr/bin/env -S dotnet run --file
#:package Lestaly.General@0.114.0
#:package RefFile@0.2.0
using Lestaly;
using Lestaly.Cx;
[assembly: RefFile(".settings.cs1")]

return await Paved.ProceedAsync(noPause: args.RoughContains("--no-pause"), async () =>
{
    await Task.CompletedTask;
    var settings = new Settings(ThisSource.Directory());
    Console.WriteLine("Service address");
    Console.WriteLine($" {Poster.Link[settings.BookStack.Url.AbsoluteUri]}");
    Console.WriteLine();
});
